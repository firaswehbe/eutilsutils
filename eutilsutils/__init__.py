# Generally follows the flow of a Flask app

import configparser

class EutilUtil:
    def __init__(self, rootlocation):
        # In Flask, config flag location can be automatically inferred
        pass


def create_app(rootlocation): 
    app = EutilUtil(rootlocation)

    # Read config


    return app
